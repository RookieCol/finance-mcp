"""MCP server entrypoint.

Tool definitions land in Stage 4 (MCP tools); this stub exists so the
`finance-mcp` console script and package layout are wired up from Stage 1
onward.
"""


def main() -> None:
    raise NotImplementedError(
        "MCP server tools land in Stage 4 — see the README status checklist for progress."
    )


if __name__ == "__main__":
    main()
